version: '3.8'
services:
  db:
    image: postgres:16
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: database
    ports:
      - "5645:5432"  # На компьютере будет порт 5645, внутри контейнера 5432
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: